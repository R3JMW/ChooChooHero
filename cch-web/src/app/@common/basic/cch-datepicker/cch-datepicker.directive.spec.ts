import { CchDatepickerDirective } from './cch-datepicker.directive';

describe('CchDatepickerDirective', () => {
  it('should create an instance', () => {
    const directive = new CchDatepickerDirective();
    expect(directive).toBeTruthy();
  });
});
